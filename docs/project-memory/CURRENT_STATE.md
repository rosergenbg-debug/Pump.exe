# CURRENT STATE

Дата: 2026-08-30

## Текущее состояние

- Создан отдельный проект спецификации Windows-лаборатории `Pump.exe`.
- Полное техническое задание находится в `ИНСТРУКЦИЯ_1.md`.
- Реализация приложения ещё не начата.
- Код и интерфейс Android PUMP этим проектом не изменялись.
- Реальные биржевые ордера запрещены текущим scope.

## Первая реализационная цель

Создать проверяемый вертикальный прототип:

`автоматическая загрузка 60 дней данных → проверка качества → причинный replay → baseline X → воспроизводимый отчёт`

## Ближайший обязательный checkpoint

До разработки GUI утвердить:

- целевую биржу и торговую пару;
- доступные форматы исходной истории X;
- правила baseline X;
- источник публичных historical trades/candles;
- политику закрытого holdout.


## Дополнение 12.09.2026 — защищённый архив исследований

По поручению владельца сохранены Windows-стратегии от +80% с исходными записями, параметрами, точными данными и восстановлением. Обязательная точка входа: [X/README.md](../../X/README.md). Вся информация выше описывает спецификацию от 30 августа; поздняя V17.1 Restored использует сохранённый движок V16. Архивирование не меняет рабочий алгоритм и не является объявлением всех результатов подтверждённой прибылью.
